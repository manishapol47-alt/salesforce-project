public with sharing class AccountClass {
    public AccountClass() {
        List<Account> lstAccounts = new List<Account>();

        //While user creating an aCcount  if user provide billing adddress but not shipping address, write a logic to 
       // populate shipping address with billing address.
       //Accont Before inser

       
    }
}